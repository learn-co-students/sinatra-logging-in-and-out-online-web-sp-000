<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
      <h1>Welcome <%="#{Helpers.current_user(session).username}"%></h1>
      <h3>Your Balance: <%="#{'%.1f' % Helpers.current_user(session).balance}"%></h3>
      <a href="/logout">Log Out</a>
    <% else %>
      <h1>You Must <a href="/">Log In</a> to View Your Balance</h1>
    <% end %>
  </body>
</html>
