<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <h1>Welcome <%= Helpers.current_user(session).username %></h1>
   <h3>Your Balance: <%= Helpers.current_user(session).balance %></h3>
   <a href="/logout">Log Out</a>
  </body>
</html>


<%-# ERB TAGS:show content of ruby string -%>
<%-# title page for account -%>
<%-# links to css stylesheets -%>
<%-# welcomes the current user using Helpers.rb method to display the username and account balance if user is logged in -%>
<%-# logout page -%>
