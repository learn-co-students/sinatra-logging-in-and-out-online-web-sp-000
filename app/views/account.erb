<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    
# this view will call helper methods
  # <% if self.is_logged_in?(session) %>
  <h1>Welcome <%= @user.username %></h1>
    <h2>Your balance is: <%= @user.balance %></h2>
    <a href="/logout">Log Out</a>
  # <% else %>
    <a href="/">Log In</a>
  # <% end %>
  
  </body>
</html>