<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
  <% if Helpers.is_logged_in?(session) == true %>
  <h2>Welcome <%= Helpers.current_user(session).username %></h2>
  <h2>Account Balance: <%= Helpers.current_user(session).balance %></h2>
  <% else %>
  <h2><a href="/">Home Page</a><h2>
  <% end %>



  <a href="/logout">Log Out</a>
    

  </body>
</html>