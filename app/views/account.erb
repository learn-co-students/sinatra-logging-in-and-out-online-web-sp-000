<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">

      <h3>Username:</h3>
      <%if Helpers.is_logged_in?(session)%>
        <p><%=Helpers.current_user(session).username%></p><br>

        <h3>Balance:</h3>
        <p><%=Helpers.current_user(session).balance%></p><br>
      <%end%>
    <a href="/logout">Log Out</a>

  </body>
</html>