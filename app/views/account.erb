<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if is_logged_in?(session) %>
      <h1>Welcome <%=current_user(session)%></h1>
      <h2><%=current_user(session).balance%></h2>
    <% else %>
      <h2> <a href="/"> <h2>
    <% end %>

    <h1> <a href="/logout">Log Out</a> <h1>
    

  </body>
</html>